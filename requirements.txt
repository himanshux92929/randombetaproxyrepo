flask>=3.0.0
curl-cffi>=0.7.0
gunicorn>=22.0.0
# Use coryking fork - maintained while original author recovers (daijro hospitalised Mar 2025)
# Installs as 'camoufox' with identical API, adds Firefox 142 support
camoufox[geoip] @ git+https://github.com/coryking/camoufox.git@v142.0.1-fork.27#subdirectory=pythonlib
